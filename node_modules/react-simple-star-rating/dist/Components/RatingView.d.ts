import React from 'react';
export interface RatingProps {
    ratingValue: number;
    stars?: number;
    size?: number;
    fillColor?: string;
    emptyColor?: string;
    className?: string;
    children?: React.ReactNode;
}
export declare function RatingView({ ratingValue, stars, size, fillColor, emptyColor, className, children }: RatingProps): JSX.Element;
