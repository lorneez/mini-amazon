/// <reference types="react" />
import { RatingProps } from './RatingView';
interface EditableRatingProps extends RatingProps {
    onClick: (index: number) => void;
    transition?: boolean;
}
export declare function Rating({ ratingValue, onClick, stars, size, transition, fillColor, emptyColor, className, children }: EditableRatingProps): JSX.Element;
export {};
